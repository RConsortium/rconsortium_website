# options specified here will apply to all posts in this folder

# Execution freezing is configured at the project level in `_quarto.yml`
# via `execute: freeze: auto`. Keeping it centralized avoids confusion.

# Enable banner style title blocks
title-block-banner: true
